<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.noname.splitsaver">

    <!-- Automatically granted permissions PROTECTION_NORMAL -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permission has to be GRANTED -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.noname.splitsaver.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Login.LoginActivity"></activity>
        <activity android:name=".Login.SignupActivity"></activity>
        <activity android:name=".Login.PinActivity"></activity>
        <activity android:name=".ImageActivity" />
        <activity
            android:name=".SplitActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".AssignmentActivity" />
        <activity android:name=".Transaction.TransactionActivity" />
        <activity android:name=".Transaction.TransactionDetailsActivity" />
        <activity android:name=".Transaction.RecipientListActivity" />
    </application>

</manifest>